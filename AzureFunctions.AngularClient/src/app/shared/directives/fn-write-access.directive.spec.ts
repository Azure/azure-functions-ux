/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { FnWriteAccessDirective } from './fn-write-access.directive';

describe('Directive: FnWriteAccess', () => {
  it('should create an instance', () => {
    let directive = new FnWriteAccessDirective(undefined);
    expect(directive).toBeTruthy();
  });
});
