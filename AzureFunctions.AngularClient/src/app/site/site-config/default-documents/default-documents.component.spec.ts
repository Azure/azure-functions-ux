import { AppModule } from './../../../app.module';
/* tslint:disable:no-unused-variable */
import { async, ComponentFixture, TestBed } from '@angular/core/testing';
import { By } from '@angular/platform-browser';
import { DebugElement } from '@angular/core';

import { DefaultDocumentsComponent } from './default-documents.component';

describe('DefaultDocumentComponent', () => {
  let component: DefaultDocumentsComponent;
  let fixture: ComponentFixture<DefaultDocumentsComponent>;

  // beforeEach(async(() => {
  //   TestBed.configureTestingModule(AppModule.moduleDefinition)
  //   .compileComponents();
  // }));

  // beforeEach(() => {
  //   fixture = TestBed.createComponent(DefaultDocumentsComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  // it('should create', () => {
  //   expect(component).toBeTruthy();
  // });
});
