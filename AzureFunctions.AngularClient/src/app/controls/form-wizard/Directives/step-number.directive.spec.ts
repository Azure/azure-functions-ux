import { StepNumberDirective } from './step-number.directive';

describe('StepNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new StepNumberDirective();
    expect(directive).toBeTruthy();
  });
});
