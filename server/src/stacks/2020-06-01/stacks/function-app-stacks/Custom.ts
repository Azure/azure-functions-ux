import { FunctionAppStack } from '../../models/FunctionAppStackModel';

export const customStack: FunctionAppStack = {
  displayText: 'Custom',
  value: 'custom',
  preferredOs: 'windows',
  majorVersions: [],
};
