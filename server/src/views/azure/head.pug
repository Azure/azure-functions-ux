- const cdn = "#{cdn}#";
- const stage = process.env['FUNCTIONS_SLOT_NAME'] === 'staging';

if config.clientOptimzationsOff || stage
    base(href="/ng-full/")
else
    script(type='text/javascript').
        window.appsvc.cdn = "#{cdn}#"
    base(href="/ng-min/")
    link(href=cdn + "/ng-min/{{styles}}" rel="stylesheet")
