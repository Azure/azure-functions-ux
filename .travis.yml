dist: trusty
sudo: required
language: node_js
node_js:
  - "8"
cache:
  directories:
  - AzureFunctions.AngularClient/node_modules
os:
- linux
before_script: 
  - npm install -g --silent @angular/cli@1.2.6
  - npm install -g --silent yarn
script:
  - cd AzureFunctions.AngularClient 
  - yarn install
  - npm run-script coveralls