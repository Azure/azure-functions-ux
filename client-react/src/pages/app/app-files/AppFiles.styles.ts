import { style } from 'typestyle';

export const commandBarSticky = style({
  position: 'sticky',
  top: 0,
  zIndex: 1,
});

export const editorStyle = style({
  marginTop: '10px',
  marginRight: '10px',
});
